[package]
name = "extender-protocol"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
postcard = { version = "1.1.3", features = ["use-std"] }
serde = { version = "1.0.228", features = ["derive"] }
